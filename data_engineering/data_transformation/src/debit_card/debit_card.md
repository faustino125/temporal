---
description: |
    API documentation for modules: data_engineering.data_transformation.src.debit_card, data_engineering.data_transformation.src.debit_card.debit_card_flow.

lang: en

classoption: oneside
geometry: margin=1in
papersize: a4

linkcolor: blue
links-as-notes: true
...



# Module `data_engineering.data_transformation.src.debit_card` {#id}





## Sub-modules

* [data_engineering.data_transformation.src.debit_card.debit_card_flow](#data_engineering.data_transformation.src.debit_card.debit_card_flow)







# Module `data_engineering.data_transformation.src.debit_card.debit_card_flow` {#id}







## Functions



### Function `debit_card_flow` {#id}




>     def debit_card_flow()


Loads, transforms and saves debit card features in the data lake.

The flow performs the following operations:
1. Loads raw cards transactions data using the specified date range.
2. Transforms and processes the debit card features data.
3. Saves the processed data to the appropriate environment using the
   specified overwrite strategy.


Returns
-----=
<code>None</code>
:   This flow does not return a value, but saves the processed


debit card features data.


### Function `process_debit_card_task` {#id}




>     def process_debit_card_task(
>         p_cleaned_debit_card: pyspark.sql.dataframe.DataFrame
>     ) ‑> pyspark.sql.dataframe.DataFrame


Transforms and processes debit card monthly data.

This task processes the clean debit card data that was previously
cleaned in the data cleaning layer

Args
-----=
**```p_cleaned_debit_card```** :&ensp;<code>DataFrame</code>
:   Cleaned debit card data.



Returns
-----=
<code>DataFrame</code>
:   Transformed base debit card DataFrame.




### Function `process_debit_card_transactions_task` {#id}




>     def process_debit_card_transactions_task(
>         p_cleaned_cards_transactions: pyspark.sql.dataframe.DataFrame,
>         p_cleaned_exchange_rate: pyspark.sql.dataframe.DataFrame
>     ) ‑> pyspark.sql.dataframe.DataFrame


Transforms and processes debit card data related to purchases.

This task processes the cleaned_card_transactions data by applying
transformations and transforming steps. It standardizes column names, creates new
columns based on transformations, and joins them into a unified dataset.


Args
-----=
**```p_cleaned_cards_transactions```** :&ensp;<code>DataFrame</code>
:   Cleaned cards transactions data.


**```p_cleaned_exchange_rate```** :&ensp;<code>DataFrame</code>
:   Cleaned exchange rate data.



Returns
-----=
<code>DataFrame</code>
:   Transformed debit card transactions features.





-----
Generated by *pdoc* 0.11.5 (<https://pdoc3.github.io>).
