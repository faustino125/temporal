---
description: |
    API documentation for modules: data_engineering.data_transformation.src.autoriza, data_engineering.data_transformation.src.autoriza.transactions_flow.

lang: en

classoption: oneside
geometry: margin=1in
papersize: a4

linkcolor: blue
links-as-notes: true
...



# Module `data_engineering.data_transformation.src.autoriza` {#id}





## Sub-modules

* [data_engineering.data_transformation.src.autoriza.transactions_flow](#data_engineering.data_transformation.src.autoriza.transactions_flow)







# Module `data_engineering.data_transformation.src.autoriza.transactions_flow` {#id}







## Functions



### Function `pivot_df_task` {#id}




>     def pivot_df_task(
>         p_df: pyspark.sql.dataframe.DataFrame,
>         p_pivot_col: str,
>         p_sum_alias: str,
>         p_count_alias: str = ''
>     ) ‑> pyspark.sql.dataframe.DataFrame


Groups a DataFrame by cuenta_corporativa and _observ_end_dt.
Then generates pivoted aggregation columns based on that groupBy statement.


Args
-----=
**```df```**
:   Dataframe that will be grouped.


**```p_pivot_col```**
:   column from dataframe used for pivot statement


**```p_sum_alias```**
:   name for sum aggregation column.


**```p_count_alias```**
:   name for count aggregation column.



Returns
-----=
<code>DataFrame</code>
:   Pivoted DataFrame.




### Function `process_atm_withdrawals_task` {#id}




>     def process_atm_withdrawals_task(
>         p_cleaned_cards_transactions: pyspark.sql.dataframe.DataFrame
>     ) ‑> pyspark.sql.dataframe.DataFrame


Transforms and processes autoriza withdrawals.


Args
-----=
**```p_cleaned_cards_transactions```** :&ensp;<code>DataFrame</code>
:   Cleaned autoriza transactions.



Returns
-----=
<code>DataFrame</code>
:   Transformed autoriza withdrawals DataFrame.




### Function `process_mcc_transactions_task` {#id}




>     def process_mcc_transactions_task(
>         p_cleaned_cards_transactions: pyspark.sql.dataframe.DataFrame
>     ) ‑> pyspark.sql.dataframe.DataFrame


Transforms and processes autoriza transactions based on the mcc group.


Args
-----=
**```p_cleaned_cards_transactions```** :&ensp;<code>DataFrame</code>
:   Cleaned autoriza transactions.



Returns
-----=
<code>DataFrame</code>
:   Transformed autoriza mcc DataFrame




### Function `process_transaction_type_task` {#id}




>     def process_transaction_type_task(
>         p_cleaned_cards_transactions: pyspark.sql.dataframe.DataFrame
>     ) ‑> pyspark.sql.dataframe.DataFrame


Transforms and processes autoriza transactions based on the transaction type.


Args
-----=
**```p_cleaned_cards_transactions```** :&ensp;<code>DataFrame</code>
:   Cleaned autoriza transactions.



Returns
-----=
<code>DataFrame</code>
:   Transformed autoriza transaction types DataFrame.




### Function `transactions_flow` {#id}




>     def transactions_flow()


Loads, transforms and saves autoriza features in the data lake.

The flow performs the following operations:
1. Loads cleaned autoriza data using the specified date range.
2. Transforms and processes the autoriza features data.
3. Saves the processed data to the appropriate environment using the
   specified overwrite strategy.


Returns
-----=
<code>None</code>
:   This flow does not return a value, but saves the processed


autoriza features data.



-----
Generated by *pdoc* 0.11.1 (<https://pdoc3.github.io>).
